import org.apache.tools.ant.filters.ReplaceTokens

buildscript {
    ext {
        jarName = "HAC.Movement.jar"
    }
}

group 'com.heretere'
version releaseVersion

shadowJar {
    destinationDirectory = shadowJarBuildPath
    archiveFileName = jarName
    relocate 'com.heretere.hac.util', 'com.heretere.hac.movement.util'
}

dependencies {
    testCompileOnly('com.github.seeseemelk:MockBukkit-v1.16:0.15.0') {
        exclude group: 'org.spigotmc'
    }

    compileOnlyApi(files('/home/heirteir/Desktop/Workspace/Libraries/Minecraft/1_11_R1.jar'))
    compileOnlyApi('net.bytebuddy:byte-buddy:1.10.18')

    api(project(':hac-util'))
    compileOnlyApi(project(':hac-core'))

    compileOnlyApi("com.google.errorprone:error_prone_annotations:2.4.0")
    errorprone("com.google.errorprone:error_prone_core:2.4.0")
}

processResources {
    duplicatesStrategy(DuplicatesStrategy.INCLUDE)
    from(sourceSets.main.resources.srcDirs) {
        filter ReplaceTokens, tokens: [version: releaseVersion]
    }
}